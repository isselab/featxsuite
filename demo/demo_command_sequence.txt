ros2 featx start_config

new terminal
------------------------------
ros2 featx load -f init_checks
ros2 featx unload -f init_checks

ros2 featx load -f to_loading
ros2 featx unload -f to_loading

ros2 featx load -f to_delivery
ros2 featx unload -f to_delivery

ros2 featx load -f to_base
ros2 featx unload -f to_base
